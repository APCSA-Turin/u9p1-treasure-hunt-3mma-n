package com.example.project;


//Dot only needs a constructor
public class Dot extends Sprite {
    // constructs a new Dot with given coordinates
    public Dot(int x, int y) {
        super(x, y);
    }
}
